# ⚡ SwiftTask DarkStore | Blinkit Operations Dashboard

> An enterprise-grade dark-store operations control system built for high-velocity inventory management, zero-config AI task parsing, and real-time algorithmic search.

---

## 🚀 Key Features

- **✨ AI Quick-Add Parser:** Natural language processing to instantly extract priorities, due dates, and task objectives from plain English.
- **📁 Project Pods & SQL Aggregation:** Group and isolate operations by pods with real-time backend group-by metrics.
- **🔍 Advanced Algorithms Engine:** Built-in implementation of Binary Search $O(\log n)$, Linear Search $O(n)$, and custom priority sorting algorithms.
- **⚡ Blinkit Dark-Store Theme:** High-contrast tactical UI optimized for high-intensity operations control.
- **📱 Responsive Architecture:** Fully adaptive layout across desktop, tablet, and mobile breakpoints.
- **🛡️ Resilient Frontend Layer:** Safe DOM updates via `createElement`, localStorage offline caching, and asynchronous REST synchronization.

---

## 🛠️ Tech Stack

- **Backend:** Python, FastAPI, SQLAlchemy, SQLite/PostgreSQL
- **Frontend:** HTML5, Modern CSS3 (Variables, Flexbox, Grid), Vanilla JavaScript (ES6+)
- **Icons & Typography:** FontAwesome 6, Google Fonts (Plus Jakarta Sans)

---

## 📂 Project Structure

```text
SwiftTask-DarkStore/
│
├── Backend/
│   ├── main.py          # FastAPI application & endpoints
│   ├── models.py        # SQLAlchemy database models
│   ├── database.py      # DB connection setup
│   └── requirements.txt # Python dependencies
│
└── Frontend/
    ├── index.html       # Single Page Application Shell
    ├── styles.css       # Blinkit design system & responsive rules
    └── app.js           # Core client-side state, API sync & DOM logic
```
